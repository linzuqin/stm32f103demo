7.19：优化了AT部分结构，AT状态机改为for循环，提高稳定性，解析平台指令部分也改为for循环遍历解析，降低了解析时代码的冗余程度，但实际稳定性还需要大量测试。

7.20：加入了时段运行部分，但未实际验证，仅用deepseek验证通过。
	  优化了easyflash部分，可在sys.h中修改使用外部flash还是内部flash，以及外部flash所挂载的spi总线，在MySPI中可以修改SPI通信的使能脚（CS）。
	知识点：SPI1总线时钟频率为72MHz（APB2）,故SPI1的理论最大速度为72/4＝18MHz； SPI2总线时钟频率为36MHz(APB1),故SPI2的理论最大速度为36/4＝9MHz。

7.21：修改了OLED的通信方式 由原来的软件iic改为了硬件iic  修改了at部分的数据结果，添加了esp和mqtt部分结构体，对定时器初始化部分做了修改，原有的占用flash太大。 

7.22：修改了freemodbus slave部分 对源码进行了剪裁 可通过sys.h文件修改需要的串口 波特率，已验证。